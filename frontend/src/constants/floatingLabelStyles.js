import Colors from './Colors';

const customLabelStyles = {
  colorFocused: Colors.primary,
  fontSizeFocused: 12,
  topFocused: -30,
  leftFocused: -10,
  leftBlurred: -10,
  colorBlurred: '#bcbcbc',
  backgroundColor: 'transparent',
};

export default customLabelStyles;
